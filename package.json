{
  "name": "rest-express",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "cd client && vite",
    "build": "cd client && vite build",
    "preview": "cd client && vite preview",
    "check": "cd client && tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {}
}
